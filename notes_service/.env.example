# Database Configuration - Using same PostgreSQL instance as auth service
DATABASE_URL=postgresql://username:password@localhost:5432/notes_app_db

# JWT Configuration - Must match auth service exactly
SECRET_KEY=your_super_secret_key
ALGORITHM=HS256

# Server Configuration
HOST=127.0.0.1
PORT=8002

# Environment
ENVIRONMENT=development
